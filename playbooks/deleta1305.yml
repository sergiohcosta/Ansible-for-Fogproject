---
- hosts: all
  gather_facts: no
  become: yes
  vars:

  tasks:
  
  - name: remove a pasta orig de dentro da imagem
    file:
      state: absent
      path: "/images/13.04-EstacaoTrabalhoMultihardware/"

